// import { EVENTS, observerEmitter } from "@/Events/Events";

import { Home, Menu, TextureButtons } from "./UI/";

export const UIcontent = () => {
  return (
    <>
      <Menu />
      <Home />
      <TextureButtons />
    </>
  );
};
